import {createClient} from "@supabase/supabase-js";import {NextResponse} from "next/server";
export async function POST(request){try{const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY,token=request.headers.get("authorization")?.replace(/^Bearer\s+/i,"");if(!url||!key||!token)return NextResponse.json({error:"Configuração inválida"},{status:401});const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}}),{data:{user}}=await admin.auth.getUser(token);const {data:pa}=await admin.from("platform_admins").select("access_role").eq("user_id",user?.id||"00000000-0000-0000-0000-000000000000").maybeSingle();if(pa?.access_role!=="full")return NextResponse.json({error:"Somente o administrador mestre."},{status:403});const b=await request.json(),email=String(b.email||"").trim().toLowerCase(),password=String(b.password||"");if(password.length<8)return NextResponse.json({error:"Senha mínima de 8 caracteres."},{status:400});let uid;const {data:created,error}=await admin.auth.admin.createUser({email,password,email_confirm:true,user_metadata:{name:b.name||"Operações"}});if(error)return NextResponse.json({error:error.message},{status:400});uid=created.user.id;const {error:e2}=await admin.from("platform_admins").insert({user_id:uid,access_role:"operations"});if(e2){await admin.auth.admin.deleteUser(uid);return NextResponse.json({error:e2.message},{status:400})}return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:500})}}